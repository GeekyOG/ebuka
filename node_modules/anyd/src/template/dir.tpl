<!DOCTYPE html>
	<html>
		<head>
			<meta charset = "utf-8">
			<title>{{title}}</title>
			<style type = "text/css">
					a,body,span{display: inline-block;margin: 30px;font-size: 30px;
						background-color: #2D2D2D;color: #FFFFFF;}
			</style>
		</head>
		<body>
			{{#each files}}
				<a href = "{{../dir}}/{{file}}"><span>[{{icon}}]</span>{{file}}</a>
			{{/each}}
			<!--
				数据是:{
					title : path.basename( filePath ),	// basename 返回文件全名(url末尾的文件全名)
					// dir : dir ? `${dir}` : "",
					dir : dir,
					files
				};
				Handlebars在each自动进入到了files中,所以在#each files中使用dir是不存在的,
				可以使用和路径操作一样的方法../回到上一层
			 -->
		</body>
	</html>
